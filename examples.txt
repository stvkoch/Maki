sugar deploy ./deploy.php
sugar deploy staging ./deploy.php
sugar deploy production ./deploy.php

sugar test ./deploy.php
sugar task_name ./deploy.php


SSH_USERNAME, SSH_PASSWORD, SSH_HOST, SSH_PORT
LOCAL_BASE, REMOTE_BASE


set(constant, value, groupname);
set(constant, value, groupname);


task('deploy', 'staging', function(){
  $resul = done('test');
  if($result){
    sugar('deploy', 'staging');
  }
});


task('test', 'subtask', function(){
  
});


task('test', function(){
  
});


//exec
exec_remote('command');
exec_local('command');

//scp
local_to_remote('file/local', 'file/remote');
remote_to_local('remote/file', 'local/file');


$result = prompt('message');
message('message');






stack